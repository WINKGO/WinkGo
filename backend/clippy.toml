# Modified from AionCore by WINK GO contributors in 2026.
disallowed-types = [
    { path = "winkgo_common::ApiError", reason = "ApiError is an HTTP/API boundary error; map crate-owned errors to it only in routes or middleware." },
]
